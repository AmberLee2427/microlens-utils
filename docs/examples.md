# Usage examples

The notebooks in this section demonstrate how to move between microlensing packages
with `microlens-utils`. They are rendered via `myst-nb` during the docs build, but
execution is disabled so they can be viewed without executing heavy dependencies.

```{toctree}
:maxdepth: 1

examples_usage
```
